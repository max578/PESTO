% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_surrogate_regime.R
\name{check_surrogate_regime}
\alias{check_surrogate_regime}
\title{Check whether a surrogate-IES regime is favourable}
\usage{
check_surrogate_regime(n_params, n_train, threshold = 5)
}
\arguments{
\item{n_params}{Integer. Number of estimated parameters.}

\item{n_train}{Integer. Number of training samples available to the
surrogate (typically the ensemble size).}

\item{threshold}{Numeric. Minimum acceptable \code{n_train / n_params}
ratio. Default \code{5}, the empirical soft floor from the surrogate-IES
vignette.}
}
\value{
Invisibly returns \code{TRUE} when the regime is favourable
(\code{n_train >= threshold * n_params}) and \code{FALSE} otherwise.
Called for the warning side-effect.
}
\description{
Issues a warning when the ratio of training points to parameters falls
below an empirical threshold, where the Gaussian-process surrogate
inside \code{\link[=pesto_surrogate_ies]{pesto_surrogate_ies()}} and \code{\link[=surrogate_ensemble_update]{surrogate_ensemble_update()}} is
unlikely to repay its training cost. The check is a soft guardrail —
it does not modify the run, only flags an unfavourable regime so the
caller can decide whether to fall back to pure IES.
}
\details{
The default threshold of \code{5} corresponds to the soft floor
\code{n_train >= 5 * n_params} documented in
\code{vignette("surrogate-ies", package = "PESTO")}. Below that floor the
GP posterior variance typically stays above the uncertainty-driven
switching threshold and surrogate savings collapse to near zero.

This is exposed as a stand-alone helper so users can call it
explicitly before scheduling an expensive ensemble. It is \strong{not}
invoked automatically by \code{\link[=pesto_surrogate_ies]{pesto_surrogate_ies()}} in the current
release; that wiring is tracked as a v0.2 enhancement candidate.
}
\examples{
# Favourable regime: 100 training points for 10 parameters.
check_surrogate_regime(n_params = 10L, n_train = 100L)

# Unfavourable regime: 30 training points for 30 parameters
# (the curse-of-dimensionality case from Scenario C of the
# comparison-and-simulation vignette). Emits a warning.
suppressWarnings(
  check_surrogate_regime(n_params = 30L, n_train = 30L)
)

# Custom threshold for users with a smoother forward model.
check_surrogate_regime(n_params = 20L, n_train = 60L, threshold = 3)

}
\references{
Rasmussen, C. E. & Williams, C. K. I. (2006). \emph{Gaussian Processes for
Machine Learning}. MIT Press.
}
\seealso{
\code{\link[=pesto_surrogate_ies]{pesto_surrogate_ies()}}, \code{\link[=surrogate_ensemble_update]{surrogate_ensemble_update()}},
\code{vignette("surrogate-ies", package = "PESTO")}
}
