% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_templates.R
\name{crop_growth_forward_model}
\alias{crop_growth_forward_model}
\title{Crop-Growth Forward Model (Logistic / Expolinear Biomass)}
\usage{
crop_growth_forward_model(
  times,
  solver = c("rk4", "desolve"),
  n_steps = 10L,
  ...
)
}
\arguments{
\item{times}{Numeric vector of strictly increasing observation times
(for example thermal-time or days-after-sowing), length at least
two. The first entry is the initial time; biomass is reported at
every later entry.}

\item{solver}{Character. \code{"rk4"} (default) or \code{"desolve"}, as in
\code{\link[=ode_forward_model]{ode_forward_model()}}.}

\item{n_steps}{Integer. Fixed RK4 sub-steps between observation times
(default \code{10L}).}

\item{...}{Further policy arguments forwarded to
\code{\link[=pesto_forward_model]{pesto_forward_model()}} via \code{\link[=ode_forward_model]{ode_forward_model()}} (for example
\code{on_failure}, \code{parallel}, \code{fidelity}).}
}
\value{
A \code{\link[=pesto_forward_model]{pesto_forward_model()}} S7 object with \code{param_names}
\code{c("r", "b_max", "b0")} and \code{n_obs = length(times) - 1L}.
}
\description{
A ready-to-use \code{\link[=ode_forward_model]{ode_forward_model()}} specialisation for the canonical
single-state crop biomass-accumulation curve. Above-ground biomass
\eqn{B} follows a logistic growth law
\deqn{\frac{dB}{dt} = r\,B\left(1 - \frac{B}{B_{\max}}\right),}
the standard sigmoid description of a crop's dry-matter accumulation
over a season: an early near-exponential phase at relative growth rate
\eqn{r}, decelerating to a canopy- and resource-limited ceiling
\eqn{B_{\max}} (Goudriaan & Monteith 1990). The calibration
parameters are the relative growth rate \code{r}, the asymptotic biomass
\code{b_max}, and the initial biomass \code{b0}.
}
\details{
The forward map integrates the logistic ODE across \code{times} and returns
the modelled biomass at every time after the first -- exactly the
shape a destructive-harvest or remote-sensing biomass series takes, so
the returned object calibrates directly against an observed
biomass-over-time vector through \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}. The
single-state logistic form is deliberately the simplest defensible
crop-growth template; richer multi-organ partitioning models compose
through the same \code{\link[=ode_forward_model]{ode_forward_model()}} entry point by supplying a
vector-valued \code{derivs}.
}
\examples{
# Simulate a biomass series at a known parameter, then recover it.
times <- seq(0, 120, by = 15)
fm <- crop_growth_forward_model(times = times)
truth <- matrix(c(0.06, 1400, 20), nrow = 1L,
                dimnames = list(NULL, c("r", "b_max", "b0")))
biomass <- as.numeric(pesto_evaluate(fm, truth))
round(biomass)
}
\references{
Goudriaan, J. & Monteith, J. L. (1990). A mathematical function for
crop growth based on light interception and leaf area expansion.
\emph{Annals of Botany}, 66(6), 695--701.
}
\seealso{
\code{\link[=ode_forward_model]{ode_forward_model()}} for the generic builder;
\code{\link[=seir_forward_model]{seir_forward_model()}} for the compartmental epidemic template;
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for calibration.
}
