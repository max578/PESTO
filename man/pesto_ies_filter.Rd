% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pesto_filter.R
\name{pesto_ies_filter}
\alias{pesto_ies_filter}
\title{Run a Sequential (Filter-Mode) Iterative Ensemble Smoother}
\usage{
pesto_ies_filter(
  forward_model,
  prior_ensemble,
  obs,
  obs_sd,
  windows,
  window_noptmax = 1L,
  lambda = 1,
  fidelity_schedule = NULL,
  parcov = NULL,
  eigthresh = 1e-06,
  use_approx = TRUE,
  inflation = NULL,
  localisation = NULL,
  on_failure = c("na", "stop"),
  verbose = TRUE
)
}
\arguments{
\item{forward_model}{A function \verb{function(theta) -> obs}, a
\code{\link[=pesto_forward_model]{pesto_forward_model()}}, or a \code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}} (then see
\code{fidelity_schedule}). The model maps the full \verb{nreal x npar} parameter
matrix to the full \verb{nreal x nobs} observation matrix; windows select
columns of that output. A bare function is auto-wrapped via
\code{\link[=as_forward_model]{as_forward_model()}}. See \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for the contract.}

\item{prior_ensemble}{Matrix or data.table, \verb{nreal x npar}.}

\item{obs}{Named numeric vector of length \code{nobs}. The full set of
target observations; \code{windows} indexes into it.}

\item{obs_sd}{Numeric scalar or length-\code{nobs} vector. Observation
standard deviation(s); weights are \code{1 / obs_sd}.}

\item{windows}{A list of integer vectors. Each element gives the
observation indices (into \code{obs}, \code{1}-based) assimilated at that
window, in assimilation order. Windows must be \strong{disjoint} (no
observation assimilated twice) and need not cover all of \code{obs}.}

\item{window_noptmax}{Integer scalar or vector. IES iterations \emph{within}
each window (default \code{1L}, the pure filter; \verb{> 1} gives an iterated
filter per window). A scalar is recycled; a short vector is
right-padded with its last value.}

\item{lambda}{Numeric scalar or per-window vector. Marquardt lambda
(default \code{1.0}; recycled / right-padded across windows).}

\item{fidelity_schedule}{Integer vector or \code{NULL}. Per-window fidelity
level for a \code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}} (see \emph{Multi-fidelity}).}

\item{parcov}{Numeric vector of length \code{npar}, the diagonal of the
prior parameter covariance. Defaults to the prior ensemble's
column-wise variance (non-positive entries replaced with \code{1}).}

\item{eigthresh}{Numeric. SVD eigenvalue truncation (default \code{1e-6}).}

\item{use_approx}{Logical. If \code{TRUE} (default) skip the prior-scaling
correction, matching the \code{pestpp-ies} default.}

\item{inflation}{A \code{\link[=pesto_inflation]{pesto_inflation()}} specification, or \code{NULL} (default).
Applied within each window's inner update to counteract ensemble
under-dispersion. See \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}.}

\item{localisation}{A \code{\link[=pesto_localisation]{pesto_localisation()}} specification, or \code{NULL}
(default). Tapers the per-window Kalman gain to suppress spurious
finite-sample correlations. See \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}.}

\item{on_failure}{Character. \code{"na"} (default) tolerates failed
realisations; \code{"stop"} aborts on any failure.}

\item{verbose}{Logical. Print a per-window phi summary.}
}
\value{
A list of class
\code{c("pesto_ies_filter_result", "pesto_ies_result")} with components:
\describe{
\item{phi}{data.table of per-realisation phi by window (on that
window's observation block).}
\item{par_ensemble}{Final parameter ensemble (data.table).}
\item{obs_ensemble}{Final simulated-observation ensemble, full
\code{nobs} columns (data.table).}
\item{windows}{List of per-window metadata: assimilated indices,
lambda, mean phi, per-parameter ensemble mean and standard
deviation (the sd trace shows the posterior tightening), and
failure count.}
\item{runtime_seconds, n_forward_evals, failure_rate}{Run totals.}
\item{fidelity}{Multi-fidelity provenance (or \code{NULL}), as in
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}}.}
}
}
\description{
Assimilates observations in time-ordered \strong{windows} against a static
parameter ensemble. Each window runs an IES Gauss-Levenberg-Marquardt
update using only that window's observation block; the updated ensemble
is carried forward as the prior for the next window. This is the
filtering analogue of \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}: instead of one batch
assimilation of all observations, the posterior is refined window by
window and is available after each.
}
\section{Filter vs smoother}{

\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} forms parameter residuals against the \emph{original}
prior mean and assimilates every observation together. Here each window
forms residuals against the \emph{current} (carried-forward) ensemble mean
and assimilates only its own block, so information accrues sequentially.
With the default \code{use_approx = TRUE} the carried-forward ensemble itself
encodes the background covariance, so the sequential behaviour comes
from propagating the ensemble, not from an explicit prior term.
}

\section{Multi-fidelity}{

When \code{forward_model} is a \code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}}, \code{fidelity_schedule}
selects the fidelity level evaluated at each \emph{window} (recycled / padded
to the number of windows; default: highest fidelity throughout). The
final ensemble refresh always uses the highest fidelity.
}

\examples{
# Linear-Gaussian recovery, assimilated in three observation windows.
set.seed(1)
npar <- 3; nobs <- 9; nreal <- 80
G <- matrix(rnorm(nobs * npar), nobs, npar)
theta_true <- c(1.0, -0.5, 2.0)
y <- as.numeric(G \%*\% theta_true) + rnorm(nobs, sd = 0.05)
f <- function(theta) theta \%*\% t(G)
prior <- matrix(rnorm(nreal * npar), nreal, npar,
                dimnames = list(NULL, paste0("p", 1:npar)))
fit <- pesto_ies_filter(
  forward_model  = f, prior_ensemble = prior,
  obs = setNames(y, paste0("o", 1:nobs)), obs_sd = 0.05,
  windows = list(1:3, 4:6, 7:9), verbose = FALSE
)
# Posterior sd should shrink window over window:
vapply(fit$windows, function(w) mean(w$par_sd), numeric(1))
}
\references{
Chen, Y. & Oliver, D.S. (2013). Levenberg-Marquardt forms of the
iterative ensemble smoother for efficient history matching and
uncertainty quantification. \emph{Computational Geosciences}, 17(4),
689--703.
}
\seealso{
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for the batch smoother;
\code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}} for fidelity stacks; \code{\link[=as_manifest]{as_manifest()}}
to wrap the result in the ensemble-manifest contract.
}
