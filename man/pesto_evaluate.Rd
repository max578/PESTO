% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward_model.R
\name{pesto_evaluate}
\alias{pesto_evaluate}
\title{Evaluate a PESTO forward model}
\usage{
pesto_evaluate(model, theta, ...)
}
\arguments{
\item{model}{A \code{pesto_forward_model} (or, for the multi-fidelity
method, a \code{pesto_multifidelity_model}).}

\item{theta}{Numeric matrix, \verb{nreal x npar}. Column names, when
present, are checked against \code{model@param_names}.}

\item{...}{Method-specific arguments. The multi-fidelity method
accepts \code{level} (integer fidelity level to evaluate).}
}
\value{
An \verb{nreal x nobs} numeric matrix with attributes
\code{"n_failures"} and \code{"fail_idx"}.
}
\description{
Runs the forward model on a parameter matrix under its own failure
policy and concurrency strategy, returning a shape-guaranteed
\verb{nreal x nobs} observation matrix. Failed realisations populate \code{NA}
rows (when \code{on_failure = "na"}). The returned matrix carries two
attributes: \code{"n_failures"} (integer count of \code{NA} rows) and
\code{"fail_idx"} (integer realisation indices that failed).
}
\examples{
fm <- pesto_forward_model(fn = function(theta) theta[, 1, drop = FALSE],
                          n_obs = 1L)
pesto_evaluate(fm, matrix(c(1, 2, 3), ncol = 1L))
}
\seealso{
\code{\link[=pesto_forward_model]{pesto_forward_model()}}, \code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}}.
}
