% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflation_localisation.R
\name{pesto_inflation}
\alias{pesto_inflation}
\title{Covariance Inflation Specification for IES}
\usage{
pesto_inflation(
  method = c("none", "rtps", "adaptive", "multiplicative"),
  alpha = 0.5,
  factor = 1,
  retention_floor = 0.5,
  max_factor = 5
)
}
\arguments{
\item{method}{Character. One of \code{"none"} (default), \code{"rtps"}, \code{"adaptive"},
\code{"multiplicative"}.}

\item{alpha}{Numeric in [0, 1]. RTPS relaxation coefficient (default 0.5);
used only when \code{method = "rtps"}.}

\item{factor}{Numeric \eqn{\ge} 1. Fixed inflation factor for
\code{method = "multiplicative"} (default 1, i.e. no inflation).}

\item{retention_floor}{Numeric in (0, 1]. Target floor on the mean
spread-retention ratio for \code{method = "adaptive"} (default 0.5).}

\item{max_factor}{Numeric \eqn{\ge} 1. Upper bound on any single-iteration
inflation factor for \code{"rtps"} and \code{"adaptive"} (default 5).}
}
\value{
An object of class \code{"pesto_inflation"}.
}
\description{
Builds a control object that tells \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} and
\code{\link[=pesto_ies_filter]{pesto_ies_filter()}} how to counteract ensemble under-dispersion --- the
progressive collapse of posterior spread that an iterative ensemble smoother
suffers with a finite ensemble. Inflation re-expands the post-update
parameter spread each iteration; the default \code{method = "none"} leaves the
update byte-identical to the un-inflated smoother.
}
\details{
Four methods are offered. \code{"rtps"} is relaxation-to-prior-spread (Whitaker &
Hamill 2012): each parameter's posterior anomalies are rescaled by
\eqn{\alpha(\sigma^{b} - \sigma^{a})/\sigma^{a} + 1}, where \eqn{\sigma^{b}}
and \eqn{\sigma^{a}} are the background (pre-update) and analysis
(post-update) standard deviations. Being per-parameter, it re-inflates the
directions that collapsed hardest, so it is the spectrally-aware workhorse.
\code{"adaptive"} is a global, magnitude-targeting scheme: it measures the mean
spread-retention ratio \eqn{q = \mathrm{mean}_j(\sigma^{a}_j/\sigma^{b}_j)}
and, when \code{q} falls below \code{retention_floor}, applies a single multiplicative
factor \eqn{\min(\texttt{max\_factor}, \texttt{retention\_floor}/q)} to
restore the lost variance magnitude. \code{"multiplicative"} applies a fixed
\code{factor} every iteration. \code{"none"} disables inflation.

The companion \emph{diagnostic} is the spectral spread-ESS
(\code{\link[=ensemble_spread_ess]{ensemble_spread_ess()}}), recorded each iteration regardless of method: it
reports the effective number of variance-carrying directions and is what
detects directional collapse. Because that participation ratio is invariant
to a global rescaling, a global (\code{"multiplicative"} / \code{"adaptive"}) inflation
restores variance \emph{magnitude} but not the spectral \emph{shape}; \code{"rtps"} is the
method that reshapes the spectrum. The two compose well.
}
\examples{
inf <- pesto_inflation("rtps", alpha = 0.5)
inf
}
\references{
Whitaker, J.S. & Hamill, T.M. (2012). Evaluating methods to account for
system errors in ensemble data assimilation. \emph{Monthly Weather Review},
140(9), 3078--3089.
}
\seealso{
\code{\link[=pesto_localisation]{pesto_localisation()}}, \code{\link[=ensemble_spread_ess]{ensemble_spread_ess()}},
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}}.
}
