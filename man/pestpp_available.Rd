% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pesto_run.R
\name{pestpp_available}
\alias{pestpp_available}
\title{Is a PEST++ Executable Available?}
\usage{
pestpp_available(which = "pestpp-ies")
}
\arguments{
\item{which}{Character scalar naming the executable to probe. Defaults to
\code{"pestpp-ies"}; any PEST++ tool name is accepted, e.g. \code{"pestpp-glm"},
\code{"pestpp-swp"}, \code{"pestpp-sen"}.}
}
\value{
A length-one logical: \code{TRUE} if the named executable is resolvable,
\code{FALSE} otherwise. Never errors.
}
\description{
A non-erroring capability probe for a PEST++ family executable. It is the
documented way for examples, vignettes, and conditional tests to skip
gracefully when no binary is installed: every PESTO algorithm runs natively
in R, so the external binaries are needed only for the optional
cross-checking and \code{.pst}-file paths.
}
\details{
The probe looks first for a copy bundled with PESTO (\code{inst/bin}), then for
the executable on the system \code{PATH}. It mirrors the resolution used by
\code{\link[=pesto_ies]{pesto_ies()}} and friends but never throws.
}
\examples{
# FALSE on a machine without PEST++ installed -- and that is fine:
pestpp_available()
pestpp_available("pestpp-glm")
}
\seealso{
\code{\link[=pesto_ies]{pesto_ies()}}, \code{\link[=pesto_glm]{pesto_glm()}}
}
