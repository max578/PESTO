% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{train_gp_surrogate}
\alias{train_gp_surrogate}
\title{Train a Gaussian Process Surrogate}
\usage{
train_gp_surrogate(
  X_train,
  Y_train,
  length_scale = 0,
  signal_var = 0,
  noise_var = 1e-04
)
}
\arguments{
\item{X_train}{Matrix (n x npar). Training parameter sets.}

\item{Y_train}{Matrix (n x nobs). Corresponding model outputs.}

\item{length_scale}{Numeric. Kernel length scale. If 0 (default),
uses the median heuristic (median pairwise distance).}

\item{signal_var}{Numeric. Signal variance. If 0, uses variance of Y.}

\item{noise_var}{Numeric. Observation noise variance.}
}
\value{
A list of class \code{step_gp} containing trained GP components:
K_inv (inverse kernel matrix), alpha (weight vectors), hyperparameters.
}
\description{
Trains a GP surrogate model from parameter-observation pairs.
Uses squared exponential (RBF) kernel with automatic relevance
determination (ARD) via median heuristic for length scale.
}
\details{
The GP learns the mapping: parameters -> observations, enabling
cheap prediction of model outputs for new parameter sets.
}
\examples{
set.seed(1L)
X_train <- matrix(rnorm(20 * 4), 20, 4)
Y_train <- matrix(rnorm(20 * 6), 20, 6)
gp <- train_gp_surrogate(X_train, Y_train)
pred <- predict_gp_surrogate(gp, X_train)
dim(pred$mean)
length(pred$uncertainty)
}
