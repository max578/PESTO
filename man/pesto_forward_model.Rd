% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward_model.R
\name{pesto_forward_model}
\alias{pesto_forward_model}
\title{Forward-Model Contract (S7 class)}
\usage{
pesto_forward_model(
  fn,
  n_obs = NA_integer_,
  param_names = character(0),
  on_failure = "na",
  parallel = "serial",
  n_cores = NA_integer_,
  map_fn = NULL,
  max_fail_frac = 1,
  fidelity = 0L,
  label = NA_character_
)
}
\arguments{
\item{fn}{Function. The forward model, signature
\verb{function(theta) -> obs}.}

\item{n_obs}{Integer or \code{NA}. Known observation dimensionality. If
\code{NA} (default) it is inferred from the first successful evaluation.}

\item{param_names}{Character. Expected parameter column names. Empty
(default) disables the column check.}

\item{on_failure}{Character. \code{"na"} (default) records failed
realisations as \code{NA} rows and proceeds; \code{"stop"} aborts on any
failure.}

\item{parallel}{Character. \code{"serial"} (default) or \code{"multicore"}.}

\item{n_cores}{Integer or \code{NA}. Worker count for \code{"multicore"}. \code{NA}
(default) resolves to \code{parallel::detectCores() - 1L} at evaluation
time.}

\item{map_fn}{Function or \code{NULL}. Optional \code{lapply}-shaped override
\verb{function(X, FUN, ...)}; when supplied it drives per-realisation
dispatch regardless of \code{parallel}.}

\item{max_fail_frac}{Numeric in \verb{[0, 1]}. Abort if the fraction of
failed realisations in any single evaluation exceeds this. Default
\code{1} (never abort on fraction; \code{on_failure} still governs the
zero-success case).}

\item{fidelity}{Integer. Fidelity level tag (default \code{0L}).}

\item{label}{Character. Optional human label carried into diagnostics.}
}
\value{
A \code{pesto_forward_model} S7 object.
}
\description{
A \code{pesto_forward_model} wraps a user callable of signature
\verb{function(theta) -> obs} -- where \code{theta} is an \verb{nreal x npar}
numeric matrix and \code{obs} is an \verb{nreal x nobs} numeric matrix -- in a
typed object that owns the evaluation contract: output dimensionality,
expected parameter names, the failure policy, the concurrency
strategy, and a fidelity tag.
}
\details{
This is the single contract both PESTO adapter modes honour. The
native callback driver \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} accepts either a bare
function (auto-wrapped via \code{\link[=as_forward_model]{as_forward_model()}} with that driver's
failure policy) or a \code{pesto_forward_model} built here; the apsimx
adapter \code{\link[=apsim_callback]{apsim_callback()}} can emit one directly. Evaluation is via
\code{\link[=pesto_evaluate]{pesto_evaluate()}}, which guarantees the returned shape and accounts
for failed realisations as \code{NA} rows.
}
\section{Concurrency}{

With \code{parallel = "serial"} (the default) and no \code{map_fn}, evaluation
attempts a single bulk call \code{fn(theta)} and falls back to a serial
per-realisation loop only if the bulk call errors -- preserving the
fast path for vectorised forward models. With \code{parallel = "multicore"}
realisations are dispatched per row through
\code{\link[parallel:mclapply]{parallel::mclapply()}} (fork-based; silently serial on Windows). A
custom \code{map_fn} (an \code{lapply}-shaped \verb{function(X, FUN, ...)}) overrides
both and lets callers plug in \code{future.apply::future_lapply},
\code{mirai}, or a cluster backend. For reproducible parallel runs set
\code{RNGkind("L'Ecuyer-CMRG")} and \code{set.seed()} before evaluating;
\code{\link[parallel:mclapply]{parallel::mclapply()}} then draws independent streams per realisation.
}

\section{Fidelity}{

\code{fidelity} is an integer level tag (\code{0L} = base / cheapest by
convention; higher = more expensive / higher resolution). A
single-fidelity model leaves it at \code{0L}. The tag is what
\code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}} uses to order levels and what the
manifest emitter threads into provenance.
}

\examples{
# A vectorised linear forward model wrapped as a contract object.
G  <- matrix(c(1, 0, 0, 1, 1, -1), nrow = 3L, byrow = TRUE)
fm <- pesto_forward_model(
  fn          = function(theta) theta \%*\% t(G),
  n_obs       = 3L,
  param_names = c("a", "b")
)
theta <- matrix(c(1, 2, 3, 4), nrow = 2L, byrow = TRUE,
                dimnames = list(NULL, c("a", "b")))
pesto_evaluate(fm, theta)
}
\seealso{
\code{\link[=pesto_evaluate]{pesto_evaluate()}} to evaluate one; \code{\link[=as_forward_model]{as_forward_model()}} to
coerce a bare function; \code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}} to compose
several across fidelity levels; \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for the IES
driver that consumes it.
}
