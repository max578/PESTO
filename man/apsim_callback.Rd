% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apsim_callback.R
\name{apsim_callback}
\alias{apsim_callback}
\title{apsimx Forward-Model Adapter for PESTO IES}
\usage{
apsim_callback(
  template,
  param_map,
  output_extractor,
  workdir = tempfile("apsim_cb_"),
  param_writer = NULL,
  simulation_runner = NULL,
  verbose = FALSE
)
}
\arguments{
\item{template}{Character. Path to a working \code{.apsimx} (Next Gen) or
\code{.apsim} (Classic) template file. Per-realisation copies are made
into \code{workdir}; the template itself is never modified.}

\item{param_map}{Named list. Names are the parameter columns expected
in \code{theta}; values are character node paths understood by the
appropriate \verb{apsimx::edit_*} function (e.g.
\code{"Manager.SowingRule.Rule.Population"} for Next Gen).}

\item{output_extractor}{Function. Takes the object returned by
\code{apsimx::apsimx()} / \code{apsim()} (typically a data.frame of report
variables) and returns a length-\code{nobs} numeric vector. The first
successful realisation defines \code{nobs}.}

\item{workdir}{Character. Per-run working directory. Created if it
does not exist; not cleaned automatically (so failures are
inspectable). Default: a fresh \code{tempfile("apsim_cb_")}.}

\item{param_writer}{Optional function with signature
\verb{function(file, src.dir, node, value)}. Overrides the default
apsimx editor dispatch. Useful for unusual node paths or
non-standard apsimx versions.}

\item{simulation_runner}{Optional function with signature
\verb{function(file, src.dir)} returning the simulation object. Overrides
the default \code{apsimx::apsimx()} / \code{apsim()} dispatch.}

\item{verbose}{Logical. Forward verbose flag into apsimx calls and
print per-realisation status (default \code{FALSE}).}
}
\value{
A closure of signature \verb{function(theta) -> obs} suitable for
the \verb{forward_model =} argument of \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}.
}
\description{
Builds an in-process forward-model closure for \code{\link[=pesto_ies_callback]{pesto_ies_callback()}}
that drives APSIM (Next Gen \code{.apsimx} or Classic \code{.apsim}) through the
\code{apsimx} package without going via the \code{.pst}-file path. Used as
Year-1 §D4 of the UQ ag-stack roadmap (\code{uq_ag_stack_roadmap_v0.md}).
}
\details{
The returned closure has signature \verb{function(theta) -> obs}, where
\code{theta} is an \verb{nreal x npar} matrix with column names matching
\code{names(param_map)}, and \code{obs} is an \verb{nreal x nobs} matrix. Each row is
produced by:
\enumerate{
\item Copying \code{template} into a fresh per-realisation file under \code{workdir}.
\item For each \verb{(par_name, node_path)} in \code{param_map}, calling the
appropriate \verb{apsimx::edit_*} function to write \code{theta[i, par_name]}
to that node.
\item Calling \code{apsimx::apsimx()} (or \code{apsim()} for Classic) and passing
the returned simulation object to \code{output_extractor()}, which must
return a length-\code{nobs} numeric vector.
}

Per-realisation failures (APSIM crash, missing output, extractor
error) populate an \code{NA} row; \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} then carries that
realisation forward unchanged or aborts, depending on its
\code{on_failure} setting.
}
\section{APSIM version compatibility}{

Tested against the \code{apsimx} package API as of CRAN 2.7.x. The exact
editor function differs between APSIM Next Gen (\verb{edit_apsimx*}) and
APSIM Classic (\code{edit_apsim}); selection is by file extension of
\code{template}. If your installed \code{apsimx} version exposes a different
editor signature, supply \code{param_writer} to override the default
per-parameter writer.
}

\section{Concurrency}{

Phase-1 D4 runs realisations \strong{serially}. Parallel execution via
\code{future} or \code{mirai} is a planned follow-up; \code{apsimx}'s thread-safety
under ensemble load has not been verified.
}

\examples{
\dontrun{
# Requires apsimx and a working APSIM installation
fm <- apsim_callback(
  template  = "wheat_wagga.apsimx",
  param_map = list(
    RUE       = "Wheat.Leaf.Photosynthesis.RUE.FixedValue",
    CN2       = "Soil.SoilWater.CN2Bare"
  ),
  output_extractor = function(sim) {
    # sim is a data.frame; extract end-of-season yield trajectory
    as.numeric(sim$Wheat.Grain.Total.Wt)
  }
)
prior <- matrix(c(runif(40, 1.0, 2.0), runif(40, 60, 90)),
                ncol = 2, dimnames = list(NULL, c("RUE", "CN2")))
fit <- pesto_ies_callback(
  forward_model  = fm,
  prior_ensemble = prior,
  obs            = c(y1 = 4500, y2 = 5200),
  obs_sd         = 200,
  noptmax        = 4
)
}
}
\seealso{
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for the IES driver; \code{\link[=pesto_ies]{pesto_ies()}}
for the classic \code{.pst}-file path.
}
