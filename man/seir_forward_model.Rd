% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_templates.R
\name{seir_forward_model}
\alias{seir_forward_model}
\title{SEIR Compartmental Forward Model}
\usage{
seir_forward_model(
  times,
  n_pop = 1000,
  i0 = 1,
  solver = c("rk4", "desolve"),
  n_steps = 10L,
  ...
)
}
\arguments{
\item{times}{Numeric vector of strictly increasing observation times
(days), length at least two. The first entry is the outbreak start.}

\item{n_pop}{Numeric. Total (closed) population size. Default \code{1000}.}

\item{i0}{Numeric. Initial infectious count at \code{times[1]}. Default
\code{1}. Must be positive and below \code{n_pop}.}

\item{solver}{Character. \code{"rk4"} (default) or \code{"desolve"}, as in
\code{\link[=ode_forward_model]{ode_forward_model()}}.}

\item{n_steps}{Integer. Fixed RK4 sub-steps between observation times
(default \code{10L}).}

\item{...}{Further policy arguments forwarded to
\code{\link[=pesto_forward_model]{pesto_forward_model()}} via \code{\link[=ode_forward_model]{ode_forward_model()}}.}
}
\value{
A \code{\link[=pesto_forward_model]{pesto_forward_model()}} S7 object with \code{param_names}
\code{c("beta", "sigma", "gamma")} and \code{n_obs = length(times) - 1L},
emitting the infectious prevalence trajectory.
}
\description{
A ready-to-use \code{\link[=ode_forward_model]{ode_forward_model()}} specialisation for the classic
Susceptible-Exposed-Infectious-Recovered epidemic model on a closed
population (Anderson & May 1991). The four states evolve as
\deqn{\dot S = -\beta S I / N,\quad
      \dot E = \beta S I / N - \sigma E,}
\deqn{\dot I = \sigma E - \gamma I,\quad \dot R = \gamma I,}
with transmission rate \eqn{\beta}, latency rate \eqn{\sigma} (mean
incubation \eqn{1/\sigma}), and recovery rate \eqn{\gamma} (mean
infectious period \eqn{1/\gamma}). The basic reproduction number is
\eqn{R_0 = \beta / \gamma}.
}
\details{
The calibration parameters are \code{beta}, \code{sigma}, and \code{gamma}. The
population size \code{n_pop} and the initial infectious count \code{i0} are
fixed structural constants of the template (an outbreak seeded with
\code{i0} infectious individuals, \code{n_pop - i0} susceptible, and nobody
exposed or recovered). By default the forward map returns the
\strong{infectious prevalence} \eqn{I(t)} at every time after the first --
the compartment a case-count series tracks -- so the object calibrates
directly against an observed epidemic curve through
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}}. Supply a custom \code{observe} through
\code{\link[=ode_forward_model]{ode_forward_model()}} to read a different compartment (for example the
incidence \eqn{\sigma E}).
}
\examples{
# Simulate an outbreak curve at a known (beta, sigma, gamma).
times <- seq(0, 60, by = 5)
fm <- seir_forward_model(times = times, n_pop = 1000, i0 = 1)
truth <- matrix(c(0.6, 0.2, 0.1), nrow = 1L,
                dimnames = list(NULL, c("beta", "sigma", "gamma")))
round(as.numeric(pesto_evaluate(fm, truth)), 1)
}
\references{
Anderson, R. M. & May, R. M. (1991). \emph{Infectious Diseases of Humans:
Dynamics and Control}. Oxford University Press.
}
\seealso{
\code{\link[=ode_forward_model]{ode_forward_model()}} for the generic builder;
\code{\link[=crop_growth_forward_model]{crop_growth_forward_model()}} for the crop template;
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for calibration.
}
