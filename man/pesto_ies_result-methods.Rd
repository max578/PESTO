% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ies_result_methods.R
\name{pesto_ies_result-methods}
\alias{pesto_ies_result-methods}
\alias{print.pesto_ies_result}
\alias{plot.pesto_ies_result}
\title{Print and plot methods for PESTO ensemble-smoother results}
\usage{
\method{print}{pesto_ies_result}(x, ...)

\method{plot}{pesto_ies_result}(x, ...)
}
\arguments{
\item{x}{A \code{pesto_ies_result}, as returned by \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} or
\code{\link[=pesto_ies_filter]{pesto_ies_filter()}}.}

\item{...}{Further arguments. For \code{plot()} these are passed to \code{\link[=plot_phi]{plot_phi()}}
(e.g. \code{log_scale}, \code{title}); \code{print()} ignores them.}
}
\value{
\code{print()} returns \code{x} invisibly; \code{plot()} returns a \code{ggplot2} object.
}
\description{
\code{print()} gives a one-screen summary of a fitted iterative ensemble-smoother
run (driver, problem dimensions, the phi-convergence trace, the spread-ESS
dispersion diagnostic and the failure rate). \code{plot()} draws the
objective-function (phi) convergence trace by delegating to \code{\link[=plot_phi]{plot_phi()}}. For
posterior parameter distributions (prior vs posterior) use \code{\link[=plot_ensemble]{plot_ensemble()}}.
}
\examples{
set.seed(1)
G <- matrix(rnorm(18L), 6L, 3L)
prior <- matrix(rnorm(150L), 50L, 3L, dimnames = list(NULL, paste0("p", 1:3)))
obs <- stats::setNames(as.numeric(G \%*\% c(1, -0.5, 2)) + rnorm(6L, sd = 0.05),
                       paste0("o", 1:6))
fit <- pesto_ies_callback(function(t) t \%*\% t(G), prior, obs,
                          obs_sd = 0.05, noptmax = 4L, verbose = FALSE)
print(fit)
p <- plot(fit)        # phi convergence (a ggplot2 object)
}
