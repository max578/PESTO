% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{surrogate_ensemble_update}
\alias{surrogate_ensemble_update}
\title{Surrogate-Accelerated Ensemble Update}
\usage{
surrogate_ensemble_update(
  par_ensemble,
  obs_ensemble,
  obs_target,
  weights,
  parcov_inv,
  cur_lam = 1,
  uncertainty_threshold = 0.1,
  eigthresh = 1e-06
)
}
\arguments{
\item{par_ensemble}{Matrix (nreal x npar). Current parameter ensemble.}

\item{obs_ensemble}{Matrix (nreal x nobs). Current observation ensemble (from model).}

\item{obs_target}{Numeric vector (nobs). Target observations.}

\item{weights}{Numeric vector (nobs). Observation weights.}

\item{parcov_inv}{Numeric vector (npar). Inverse parameter covariance diagonal.}

\item{cur_lam}{Numeric. Marquardt lambda.}

\item{uncertainty_threshold}{Numeric. Threshold for surrogate/model switching.
Realisations with GP uncertainty above this are re-evaluated with full model.
Default 0.1 (10\% of signal variance).}

\item{eigthresh}{Numeric. SVD eigenvalue threshold.}
}
\value{
A list with:
\describe{
\item{upgrade}{Matrix. Parameter upgrades.}
\item{n_model_runs}{Integer. Number of full model evaluations needed.}
\item{n_surrogate_runs}{Integer. Number of surrogate evaluations.}
\item{savings_pct}{Numeric. Percentage of model runs saved.}
\item{gp_diagnostics}{List. GP training diagnostics.}
}
}
\description{
Performs an IES update step using a GP surrogate for cheap
pre-screening, with adaptive switching to the full model
based on prediction uncertainty.
}
\details{
\strong{Algorithm:}
\enumerate{
\item Train GP surrogate from current ensemble evaluations
\item Generate candidate upgrades using surrogate predictions
\item Evaluate uncertainty of surrogate predictions
\item Run full model only for realisations where uncertainty exceeds threshold
\item Blend surrogate and model results using control-variate correction
}

This typically reduces full model evaluations by 50-90\%.
}
