% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pesto_reference_ies.R
\name{pesto_reference_ies}
\alias{pesto_reference_ies}
\title{Pure-R Reference IES Update — Chen & Oliver (2013) eq. 12}
\usage{
pesto_reference_ies(
  par_ensemble,
  obs_ensemble,
  obs_target,
  weights,
  lambda = 1
)
}
\arguments{
\item{par_ensemble}{Numeric matrix \verb{(n_par x n_real)}. Current
parameter ensemble (each column is one realisation).}

\item{obs_ensemble}{Numeric matrix \verb{(n_obs x n_real)}. Simulated
observations from the current ensemble.}

\item{obs_target}{Numeric vector \code{(n_obs)}. Target (measured)
observation values.}

\item{weights}{Numeric vector \code{(n_obs)}. Observation weights
(typically \code{1 / sd(obs_noise)}).}

\item{lambda}{Numeric scalar. Marquardt damping parameter. \code{1.0} is
the textbook GLM update; larger values dampen more aggressively.}
}
\value{
A numeric matrix \verb{(n_par x n_real)}: the parameter upgrade
(add to \code{par_ensemble} to get the next iterate).
}
\description{
A textbook, pure-R implementation of one Iterative Ensemble Smoother
(IES) parameter upgrade as published in Chen & Oliver (2013) eq. 12.
This function is independent of \code{\link[=ensemble_solution]{ensemble_solution()}} (the C++
kernel) and exists for two purposes:
}
\details{
\enumerate{
\item \strong{Independent numerical validation.} PESTO's C++ kernel can be
cross-checked against this reference at machine precision (the
package's regression test
\code{tests/testthat/test-ensemble-solution-sign.R} and the
paired-seed protocol in \code{inst/scripts/i2_paired_seed_check.R}
both rely on this).
\item \strong{Self-contained \code{pestpp-ies} comparison.} Vignettes can compare
PESTO native IES to this textbook reference without requiring the
upstream \code{pestpp-ies} binary, making the comparison story
reproducible on CRAN check farms and other binary-free
environments.
}

The implementation is deliberately pedagogical — readability over
speed. For production work use \code{\link[=ensemble_solution]{ensemble_solution()}} (the C++
kernel; \eqn{\sim 35\times} faster on production-scale ensembles).
}
\section{Sign convention}{

This reference uses the textbook sign \code{obs_resid = obs - sim} with a
positive leading sign in the upgrade. PESTO's C++ kernel uses the
equivalent \code{obs_resid = sim - obs} with a leading negative sign
(see \code{?ensemble_solution}). Both yield identical upgrades; the
difference is purely cosmetic. The two are cross-validated to
machine precision in
\code{inst/scripts/i2_paired_seed_check.R}.
}

\examples{
# Simple linear inverse problem: identify the true theta given noisy obs.
set.seed(20260425L)
n_par <- 4L; n_obs <- 12L; n_real <- 30L
G <- matrix(rnorm(n_obs * n_par) / sqrt(n_par), n_obs, n_par)
theta_true <- rnorm(n_par)
y_obs <- as.numeric(G \%*\% theta_true) + rnorm(n_obs, sd = 0.05)
weights <- rep(1 / 0.05, n_obs)

par_ens <- matrix(rnorm(n_par * n_real, sd = 1), n_par, n_real)
obs_ens <- G \%*\% par_ens

# One textbook IES upgrade.
upg <- pesto_reference_ies(
  par_ensemble = par_ens,
  obs_ensemble = obs_ens,
  obs_target   = y_obs,
  weights      = weights,
  lambda       = 1.0
)
dim(upg)

# Apply the upgrade and check phi reduces.
par_next <- par_ens + upg
obs_next <- G \%*\% par_next
Y <- matrix(rep(y_obs, n_real), n_obs, n_real)
phi0 <- mean(compute_phi(Y - obs_ens,  weights))
phi1 <- mean(compute_phi(Y - obs_next, weights))
phi1 < phi0

}
\references{
Chen, Y. & Oliver, D. S. (2013). Levenberg-Marquardt forms of the
iterative ensemble smoother for efficient history matching and
uncertainty quantification. \emph{Computational Geosciences}, 17(4),
689--703. \doi{10.1007/s10596-013-9351-5}

Evensen, G. (2018). Analysis of iterative ensemble smoothers for
solving inverse problems. \emph{Computational Geosciences}, 22(3),
885--908. \doi{10.1007/s10596-018-9731-y}
}
\seealso{
\code{\link[=ensemble_solution]{ensemble_solution()}} for the production C++ kernel.
}
