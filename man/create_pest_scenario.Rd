% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{create_pest_scenario}
\alias{create_pest_scenario}
\title{Create a PEST Scenario Programmatically}
\usage{
create_pest_scenario(
  parameters,
  observations,
  model_command,
  template_files = NULL,
  instruction_files = NULL,
  pestpp_options = list()
)
}
\arguments{
\item{parameters}{data.table. Parameter definitions with columns:
\code{parnme}, \code{partrans}, \code{parchglim}, \code{parval1}, \code{parlbnd}, \code{parubnd}, \code{pargp}.}

\item{observations}{data.table. Observation definitions with columns:
\code{obsnme}, \code{obsval}, \code{weight}, \code{obgnme}.}

\item{model_command}{Character. Model command line(s).}

\item{template_files}{data.table. Template/model input file pairs
with columns \code{template_file}, \code{model_file}.}

\item{instruction_files}{data.table. Instruction/model output file pairs
with columns \code{instruction_file}, \code{model_file}.}

\item{pestpp_options}{Named list. PEST++ options.}
}
\value{
A \code{pesto_pst} object.
}
\description{
Builds a \code{pesto_pst} object from R data structures, without
requiring an existing .pst file.
}
\examples{
pars <- data.table::data.table(
  parnme = c("k1", "k2"),
  partrans = "log",
  parchglim = "factor",
  parval1 = c(1.0, 0.5),
  parlbnd = c(0.01, 0.001),
  parubnd = c(100, 50),
  pargp = "hydraulic"
)
obs <- data.table::data.table(
  obsnme = c("h1", "h2"),
  obsval = c(1.0, 2.0),
  weight = c(1.0, 1.0),
  obgnme = "head"
)
pst <- create_pest_scenario(
  parameters    = pars,
  observations  = obs,
  model_command = "python model.py"
)
inherits(pst, "pesto_pst")
pst$control_data$npar
}
