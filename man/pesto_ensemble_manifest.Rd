% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{pesto_ensemble_manifest}
\alias{pesto_ensemble_manifest}
\title{PESTO Ensemble Manifest (S7 class)}
\arguments{
\item{schema_version}{Character. Semantic version of the manifest
schema itself (default \code{"1.1.0"}; \verb{1.1.0} adds the optional
\code{obs_schema} grounding descriptor over \verb{1.0.0}).}

\item{run_id}{Character. Short identifier (auto-generated by
\code{\link[=as_manifest]{as_manifest()}} if not supplied).}

\item{obs_schema}{Optional grounded semantic descriptor (or \code{NULL}).
A \verb{list(outputs = <data.frame>, params = <data.frame>)} giving the
\emph{meaning} of each output / parameter column so downstream consumers
can check correspondence (unit + quantity) by name rather than
trusting positional convention. \code{outputs} columns: \code{name},
\code{quantity}, \code{unit} (+ optional provenance \code{verified_on},
\code{oracle_kind}, \code{evidence_path}); \code{params} columns: \code{name},
\code{apsim_node}, \code{unit} (+ same optional provenance). Build one with
\code{\link[=pesto_obs_schema]{pesto_obs_schema()}}. The descriptor is provenance metadata — it is
\strong{not} folded into \code{data_hash} (correspondence is grounded by a
second member that agrees on units, not by self-hash; see the
orchestra provenance vocabulary). Default \code{NULL} (absent).}

\item{params}{\code{data.frame} / \code{data.table}. Parameter ensemble; first
column \code{real_name}, subsequent columns one per parameter.}

\item{outputs}{\code{data.frame} / \code{data.table}. Simulated observation
ensemble (same shape conventions as \code{params}).}

\item{weights}{Named numeric. IES observation weights (\code{1/obs_sd}).}

\item{obs_target}{Named numeric. Target observations.}

\item{seed}{Integer scalar. RNG seed used (\code{NA_integer_} if
unrecorded).}

\item{data_hash}{Character. \code{sha256:...} checksum over
\verb{(params, outputs, weights, obs_target, seed)}.}

\item{fidelity}{Structured provenance list or \code{NULL}. For a
multi-fidelity run \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} records
\code{list(type, schedule, final_level, n_levels, costs)}; \code{NULL} for a
single-fidelity run. A legacy named-numeric tag is still accepted.}

\item{apsim_version}{Character. APSIM version string
(\code{NA_character_} when forward model is non-APSIM).}

\item{pesto_version}{Character. PESTO package version that produced
the run.}

\item{timestamp}{\code{POSIXct}. Construction time.}

\item{method}{Character. One of \code{"ies_callback"}, \code{"ies_pst"},
\code{"mda"}, \code{"surrogate"}.}

\item{noptmax}{Integer. Number of IES iterations.}

\item{lambda_schedule}{Numeric vector. Marquardt lambdas per
iteration.}

\item{failure_rate}{Numeric in \verb{[0, 1]}. Fraction of forward
evaluations that returned \code{NA}.}
}
\description{
A versioned, hashed, provenance-tracked container for the result of a
PESTO ensemble run. The manifest is the cross-package contract object
that downstream consumers (\code{kernR}, \code{proxymix}, paper-skill) read; it
carries enough provenance to make a run independently reproducible and
independently verifiable (via \code{\link[=verify_manifest]{verify_manifest()}}).
}
\details{
Construct directly via the class constructor, or — preferred — via
\code{\link[=as_manifest]{as_manifest()}} applied to a \code{pesto_ies_callback_result}.
}
\section{Persistence}{

See \code{\link[=write_manifest]{write_manifest()}} / \code{\link[=read_manifest]{read_manifest()}} for YAML serialisation
and \code{\link[=verify_manifest]{verify_manifest()}} for integrity checking. The YAML file is the
authoritative human-readable entry; \code{params}, \code{outputs}, and the
assimilation context (\code{weights} + \code{obs_target}) are emitted as
sidecar files whose relative paths are recorded inside the YAML.
Three sidecar modes:
\itemize{
\item \code{format = "rds"} (default, \strong{verifiable}) — RDS only. IEEE 754
doubles round-trip bit-exactly; SHA-256 integrity check holds.
\item \code{format = "both"} (\strong{verifiable}) — RDS sidecars plus parallel
\verb{*_inspection.csv} files. Hash bound to RDS; CSVs are decorative.
\item \code{format = "csv_unverified"} (\strong{not verifiable}, renamed from
\code{"csv"} in PESTO 0.3.2) — CSV-only sidecars. Hash is recorded
but cannot be recomputed from disk (CSV formatter precision loss
~1 ULP). \code{\link[=verify_manifest]{verify_manifest()}} returns \code{ok = NA + message}. Use
only for one-way export to non-R analysts; round-trip integrity
is by construction broken.
}

The YAML carries an explicit \verb{integrity:} field
(\code{"verifiable"} / \code{"not_verifiable"}) derived from \code{format} so
downstream non-R tools can branch on the integrity contract
without needing to know the kernR/PESTO-specific format
vocabulary.
}

\examples{
set.seed(1)
npar <- 2L; nobs <- 3L; nreal <- 30L
G <- matrix(stats::rnorm(nobs * npar), nobs, npar)
y <- as.numeric(G \%*\% c(0.5, -1.0)) + stats::rnorm(nobs, sd = 0.05)
names(y) <- paste0("o", seq_len(nobs))
prior <- matrix(stats::rnorm(nreal * npar), nreal, npar,
                dimnames = list(NULL, c("a", "b")))
fit <- pesto_ies_callback(function(t) t \%*\% t(G), prior, y, 0.05,
                          noptmax = 3, verbose = FALSE)
m <- as_manifest(fit, seed = 1L)
verify_manifest(m)$ok
}
