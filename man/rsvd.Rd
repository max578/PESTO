% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rsvd}
\alias{rsvd}
\title{Randomised SVD (Halko-Martinsson-Tropp Algorithm)}
\usage{
rsvd(A, k, p = 10L, q = 2L)
}
\arguments{
\item{A}{Matrix (m x n). Input matrix.}

\item{k}{Integer. Target rank (number of singular values to compute).}

\item{p}{Integer. Oversampling parameter (default 10). Higher = more accurate.}

\item{q}{Integer. Number of power iterations (default 2). Higher = better for
matrices with slowly decaying singular values.}
}
\value{
A list with components U (m x k), d (k), V (n x k).
}
\description{
Computes a rank-k approximation to the SVD using randomised
projections. This is asymptotically faster than full SVD for
problems where the target rank k is much smaller than min(m,n).
}
\details{
Complexity: O(mn\emph{k) vs O(mn}min(m,n)) for full SVD.
}
\examples{
set.seed(1L)
A <- matrix(rnorm(10 * 6), nrow = 10, ncol = 6)
res <- rsvd(A, k = 3L)
length(res$d)
A_hat <- res$u \%*\% diag(res$d) \%*\% t(res$v)
mean((A - A_hat)^2)
}
\references{
Halko, N., Martinsson, P.G., & Tropp, J.A. (2011). Finding structure
with randomness: Probabilistic algorithms for constructing approximate
matrix decompositions. SIAM Review, 53(2), 217-288.
}
