% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble_io.R
\name{read_ensemble}
\alias{read_ensemble}
\title{Read an Ensemble File}
\usage{
read_ensemble(file, format = c("csv", "binary"))
}
\arguments{
\item{file}{Character. Path to ensemble file.}

\item{format}{Character. One of "csv" (default) or "binary".}
}
\value{
A data.table with realisations as rows and parameters/observations as columns.
The first column \code{real_name} contains realisation names.
}
\description{
Reads PEST++ ensemble files in CSV or binary (.jcb/.jco) format.
}
\examples{
ens <- data.table::data.table(
  real_name = sprintf("real_\%02d", 1:10),
  k1 = rnorm(10, mean = 1.0, sd = 0.2),
  k2 = rnorm(10, mean = 0.5, sd = 0.1),
  k3 = rnorm(10, mean = 2.0, sd = 0.3)
)
tf <- tempfile(fileext = ".csv")
on.exit(unlink(tf), add = TRUE)
write_ensemble(ens, tf)
ens_back <- read_ensemble(tf, format = "csv")
identical(names(ens_back), names(ens))
nrow(ens_back) == nrow(ens)
}
\seealso{
\code{\link[=write_ensemble]{write_ensemble()}}
}
