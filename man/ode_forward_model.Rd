% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_templates.R
\name{ode_forward_model}
\alias{ode_forward_model}
\title{Forward Model from a System of Ordinary Differential Equations}
\usage{
ode_forward_model(
  derivs,
  y0,
  times,
  param_names = character(0),
  observe = NULL,
  solver = c("rk4", "desolve"),
  n_steps = 10L,
  desolve_method = "lsoda",
  n_obs = NA_integer_,
  ...
)
}
\arguments{
\item{derivs}{Function \verb{function(t, y, theta) -> dydt}. The ODE right
hand side; returns a numeric vector the same length as the state
\code{y}. \code{theta} is the named parameter vector for one realisation.}

\item{y0}{Numeric vector, or \verb{function(theta) -> numeric}. The initial
state at \code{times[1]}. A function form lets the initial condition
depend on calibrated parameters.}

\item{times}{Numeric vector of strictly increasing observation times,
length at least two. The first entry is the initial time; the
trajectory is recorded at every entry.}

\item{param_names}{Character vector of the parameter columns the model
consumes from \code{theta}, in order. Empty (default) disables the column
check and passes \code{theta} rows through verbatim.}

\item{observe}{Function \verb{function(traj, theta) -> obs}. Maps the
\verb{length(times) x n_state} trajectory matrix to a length-\code{n_obs}
observation vector. Default reads state variable one at every time
after the first.}

\item{solver}{Character. \code{"rk4"} (default, self-contained) or
\code{"desolve"} (delegates to \code{deSolve::ode()}).}

\item{n_steps}{Integer. Fixed RK4 sub-steps between successive
observation times (default \code{10L}). Ignored for \code{solver = "desolve"}.
Larger values trade speed for integration accuracy.}

\item{desolve_method}{Character. The \code{deSolve::ode()} method when
\code{solver = "desolve"} (default \code{"lsoda"}). Ignored for \code{"rk4"}.}

\item{n_obs}{Integer or \code{NA}. Known observation dimensionality. \code{NA}
(default) infers it from the first successful realisation.}

\item{...}{Further policy arguments forwarded to
\code{\link[=pesto_forward_model]{pesto_forward_model()}} (for example \code{on_failure}, \code{parallel},
\code{n_cores}, \code{fidelity}, \code{label}).}
}
\value{
A \code{\link[=pesto_forward_model]{pesto_forward_model()}} S7 object whose forward map
integrates the ODE system. Pass it to \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} as
\code{forward_model}, evaluate it directly with \code{\link[=pesto_evaluate]{pesto_evaluate()}}, or
bundle several across fidelity levels with
\code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}}.
}
\description{
Each calibration parameter is a named entry of the \code{theta} matrix
supplied by the IES driver. \code{param_names} declares which columns the
model consumes and in what order. For each realisation the template
integrates
\deqn{\frac{d\mathbf{y}}{dt} = f(t, \mathbf{y}, \theta)}
from \code{times[1]} with initial state \code{y0} -- itself possibly a function
of \code{theta} -- and applies \code{observe()} to the state trajectory to
produce a length-\code{n_obs} numeric vector.
}
\details{
Wraps a user-supplied ODE right-hand side in a typed
\code{\link[=pesto_forward_model]{pesto_forward_model()}} whose forward map integrates the system over a
fixed time grid and reads the observation vector off the resulting
state trajectory. This is the generic \code{differential_equations}
template that \code{\link[=crop_growth_forward_model]{crop_growth_forward_model()}} and
\code{\link[=seir_forward_model]{seir_forward_model()}} specialise; use it directly for any
compartmental or mechanistic ODE.

The derivative function \code{derivs} has signature
\verb{function(t, y, theta) -> dydt}, returning a numeric vector the same
length as the state \code{y}. The initial state \code{y0} may be a plain numeric
vector (shared across realisations) or a function
\verb{function(theta) -> y0} when the starting condition is itself
calibrated (for example an unknown initial inoculum). The observation
map \code{observe} has signature \verb{function(traj, theta) -> obs}, where
\code{traj} is an \verb{length(times) x length(y0)} matrix of states at each
integration time (column order matching \code{y0}); the default reads the
first state variable at every time after the first.

Two integrators are available. The default \code{solver = "rk4"} is a
self-contained classical fourth-order Runge-Kutta with \code{n_steps}
fixed sub-steps between successive observation times -- no external
dependency, deterministic, and adequate for the smooth non-stiff
systems these templates target. \code{solver = "desolve"} delegates to
\code{deSolve::ode()} (an optional \code{Suggests} dependency) with method
\code{desolve_method}, which brings adaptive step control and stiff solvers
(\code{"lsoda"}, the default, switches between non-stiff and stiff
automatically). A realisation whose integration fails or returns a
non-finite trajectory is reported as an \code{NA} row, which
\code{\link[=pesto_evaluate]{pesto_evaluate()}} and \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} handle under their
failure policy.
}
\examples{
# Exponential decay dy/dt = -k y, observed at five times. Calibrate k.
fm <- ode_forward_model(
  derivs      = function(t, y, theta) -theta[["k"]] * y,
  y0          = c(y = 1),
  times       = seq(0, 4, by = 1),
  param_names = "k"
)
theta <- matrix(c(0.5, 1.0), ncol = 1L, dimnames = list(NULL, "k"))
pesto_evaluate(fm, theta)
}
\references{
Soetaert, K., Petzoldt, T. & Setzer, R. W. (2010). Solving
differential equations in R: package deSolve. \emph{Journal of Statistical
Software}, 33(9), 1--25. \doi{10.18637/jss.v033.i09}
}
\seealso{
\code{\link[=crop_growth_forward_model]{crop_growth_forward_model()}} and \code{\link[=seir_forward_model]{seir_forward_model()}} for
ready-made specialisations; \code{\link[=pesto_forward_model]{pesto_forward_model()}} for the contract;
\code{\link[=pesto_ies_callback]{pesto_ies_callback()}} for the IES driver.
}
