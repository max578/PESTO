% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogate.R
\name{pesto_surrogate_ies}
\alias{pesto_surrogate_ies}
\title{Surrogate-Accelerated IES Iteration}
\usage{
pesto_surrogate_ies(
  par_ensemble,
  obs_ensemble,
  obs_target,
  weights,
  parcov_inv,
  lambda = 1,
  uncertainty_threshold = 0.1,
  eigthresh = 1e-06
)
}
\arguments{
\item{par_ensemble}{data.table or matrix. Current parameter ensemble
(rows = realisations, columns = parameters).}

\item{obs_ensemble}{data.table or matrix. Current observation ensemble
from model evaluations.}

\item{obs_target}{Named numeric vector. Target observation values.}

\item{weights}{Numeric vector. Observation weights.}

\item{parcov_inv}{Numeric vector. Diagonal of inverse parameter covariance.}

\item{lambda}{Numeric. Marquardt lambda (default 1.0).}

\item{uncertainty_threshold}{Numeric. Threshold for surrogate/model switching.
Fraction of signal variance (default 0.1 = 10\%).}

\item{eigthresh}{Numeric. SVD eigenvalue threshold.}
}
\value{
A list containing:
\describe{
\item{upgrade}{Matrix of parameter upgrades}
\item{n_model_runs}{Number of full model evaluations needed}
\item{n_surrogate_runs}{Number of surrogate-only evaluations}
\item{savings_pct}{Percentage of model runs saved}
\item{gp_diagnostics}{GP training diagnostics}
}
}
\description{
Performs a single IES iteration using a Gaussian Process surrogate
to reduce the number of expensive full-model evaluations.
}
\details{
\strong{How it works:}
\enumerate{
\item A GP surrogate is trained on existing parameter-observation pairs
\item The surrogate predicts model outputs for all ensemble members
\item Only members with high prediction uncertainty trigger full model runs
\item Control-variate bias correction blends surrogate and model results
\item Standard IES update is computed on the blended ensemble
}

This typically saves 50-90\% of model evaluations per iteration.
}
\references{
Rasmussen, C.E. & Williams, C.K.I. (2006). Gaussian Processes for
Machine Learning. MIT Press.

Liu, F. & Guillas, S. (2017). Dimension reduction for Gaussian process
emulation. \emph{Statistics and Computing}, 27(3), 785-802.
}
