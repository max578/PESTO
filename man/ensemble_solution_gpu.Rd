% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{ensemble_solution_gpu}
\alias{ensemble_solution_gpu}
\title{Deprecated: ensemble solution with adaptive SVD backend}
\usage{
ensemble_solution_gpu(...)
}
\arguments{
\item{...}{Arguments passed on to \code{\link[=ensemble_solution_adaptive]{ensemble_solution_adaptive()}}.}
}
\value{
The list returned by \code{\link[=ensemble_solution_adaptive]{ensemble_solution_adaptive()}}.
}
\description{
\code{ensemble_solution_gpu()} was renamed to \code{\link[=ensemble_solution_adaptive]{ensemble_solution_adaptive()}}.
The original name implied GPU computation; the function performs CPU
adaptive-SVD backend selection (randomised SVD versus a dense LAPACK /
Accelerate decomposition). The alias is retained for backward compatibility
and will be removed in a future release.
}
\examples{
set.seed(1L)
np <- 4L; no <- 30L; nr <- 20L
pd <- matrix(rnorm(np * nr), np, nr)
od <- matrix(rnorm(no * nr), no, nr)
or_ <- matrix(rnorm(no * nr, sd = 0.5), no, nr)
Am <- matrix(0, 0, 0)
suppressWarnings(
  res <- ensemble_solution_gpu(pd, od, or_, pd, rep(1, no), rep(1, np),
                               Am, cur_lam = 1.0)
)
dim(res$upgrade)
}
\seealso{
\code{\link[=ensemble_solution_adaptive]{ensemble_solution_adaptive()}}
}
