% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multifidelity.R
\name{mf_control_variate}
\alias{mf_control_variate}
\title{Affine control-variate bias correction across fidelities}
\usage{
mf_control_variate(low_all, high_sub, low_sub)
}
\arguments{
\item{low_all}{Numeric matrix, \verb{nreal x nobs}. Cheap-fidelity output
for every realisation.}

\item{high_sub}{Numeric matrix, \verb{nsub x nobs}. Expensive-fidelity
output for the paired subset.}

\item{low_sub}{Numeric matrix, \verb{nsub x nobs}. Cheap-fidelity output
for the same subset, row-aligned with \code{high_sub}.}
}
\value{
A \verb{nreal x nobs} matrix of bias-corrected outputs, with
attributes \code{"intercept"} (\code{a_j}), \code{"slope"} (\code{b_j}), and
\code{"subset_cor"} (per-dimension subset correlation; \code{NA} for a
degenerate dimension).
}
\description{
Debiases a cheap-fidelity output ensemble against a sparse expensive
sample, per observation dimension. For each observation \code{j} it fits
the first-order autoregressive control variate (the linear term of
the Kennedy-O'Hagan multi-fidelity model)
\code{high_j ~ a_j + b_j * low_j} on the paired subset, with
\code{b_j = cov(high_j, low_j) / var(low_j)} the variance-minimising
coefficient, then predicts the corrected high-fidelity output for
every realisation as \code{a_j + b_j * low_all_j}.
}
\details{
The estimator degrades gracefully: where the cheap output has zero
variance on the subset it falls back to the expensive subset mean
(\code{b_j = 0}), and where the two fidelities are weakly correlated the
correction shrinks toward that mean rather than amplifying noise.

This is the plug-in primitive for surrogate cascades: a cascade runs
the cheap level over the full ensemble, the expensive level over a
chosen subset, and calls this to lift the cheap ensemble toward the
expensive one at a fraction of the cost.
}
\examples{
set.seed(1L)
low_all  <- matrix(rnorm(40L), ncol = 2L)
sub      <- 1:5
low_sub  <- low_all[sub, , drop = FALSE]
high_sub <- 0.3 + 1.2 * low_sub + matrix(rnorm(10L, sd = 0.01), ncol = 2L)
corrected <- mf_control_variate(low_all, high_sub, low_sub)
attr(corrected, "slope")
}
\references{
Kennedy, M. C. & O'Hagan, A. (2000). Predicting the output from a
complex computer code when fast approximations are available.
\emph{Biometrika}, 87(1), 1--13.
}
\seealso{
\code{\link[=pesto_multifidelity_model]{pesto_multifidelity_model()}}.
}
