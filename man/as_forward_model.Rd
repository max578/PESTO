% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward_model.R
\name{as_forward_model}
\alias{as_forward_model}
\title{Coerce an object into a \code{pesto_forward_model}}
\usage{
as_forward_model(x, ...)
}
\arguments{
\item{x}{A function or a \code{pesto_forward_model}.}

\item{...}{Policy arguments forwarded to \code{\link[=pesto_forward_model]{pesto_forward_model()}} when
\code{x} is a bare function (e.g. \code{on_failure}, \code{parallel}, \code{n_obs}).}
}
\value{
A \code{pesto_forward_model} S7 object.
}
\description{
Generic used by the IES driver so a caller can pass either a bare
\verb{function(theta) -> obs} or a fully-specified \code{\link[=pesto_forward_model]{pesto_forward_model()}}.
A bare function is wrapped with the supplied policy arguments; an
existing \code{pesto_forward_model} is returned unchanged (its own policy
is authoritative and the \code{...} are ignored).
}
\examples{
fm <- as_forward_model(function(theta) theta, on_failure = "stop")
S7::S7_inherits(fm, pesto_forward_model)
}
\seealso{
\code{\link[=pesto_forward_model]{pesto_forward_model()}}, \code{\link[=pesto_evaluate]{pesto_evaluate()}}.
}
