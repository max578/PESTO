% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{pesto_obs_schema}
\alias{pesto_obs_schema}
\title{Build a grounded \code{obs_schema} descriptor for a manifest}
\usage{
pesto_obs_schema(outputs = NULL, params = NULL)
}
\arguments{
\item{outputs}{Optional \code{data.frame} describing output columns, with
columns \code{name}, \code{quantity}, \code{unit} (character) and optionally
\code{verified_on} (\code{Date}), \code{oracle_kind}, \code{evidence_path}.}

\item{params}{Optional \code{data.frame} describing parameter columns, with
columns \code{name}, \code{apsim_node}, \code{unit} and the same optional
provenance columns.}
}
\value{
A validated \code{list(outputs = , params = )} suitable for the
\code{obs_schema} argument of \link{pesto_ensemble_manifest}. Either side may
be omitted (\code{NULL}).
}
\description{
Constructs the optional \code{obs_schema} slot of a
\link{pesto_ensemble_manifest}: a machine-checkable statement of what each
output and parameter column \emph{means} (its physical quantity and unit),
so a downstream consumer can verify two manifests are commensurable by
name rather than trusting a positional convention. This is the single
Independent-Oracle anchor the manifest contract previously lacked.
}
\details{
Each row optionally carries provenance per the orchestra provenance
vocabulary: \code{verified_on} (a \code{Date}, or \code{NA} for an unverified fact),
\code{oracle_kind}, and \code{evidence_path}. A column is \emph{grounded} only when
\code{verified_on} is a non-\code{NA} date.
}
\seealso{
The manifest contract \code{inst/manifest_contract.md} and the
orchestra provenance vocabulary.
}
