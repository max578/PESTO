% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abstention.R
\name{pesto_abstention}
\alias{pesto_abstention}
\title{Construct a typed PESTO abstention}
\usage{
pesto_abstention(
  reason,
  detail = NA_character_,
  diagnostics = list(),
  scope = "call"
)
}
\arguments{
\item{reason}{Character scalar reason code. One of \code{"degenerate_ensemble"}
(fewer than two realisations survived a step and the update cannot be
formed), \code{"over_determined"} (the ensemble is too small relative to the
number of parameters to support the update -- \code{nreal <= npar}), or
\code{"surrogate_off_design"} (the GP surrogate's training-point-to-parameter
ratio falls below \code{\link[=check_surrogate_regime]{check_surrogate_regime()}}'s favourable floor).}

\item{detail}{Character scalar human-readable detail, or \code{NA}.}

\item{diagnostics}{Named list of scalar diagnostics supporting the
decline (e.g. \code{list(iteration = 3L, n_ok = 1L, nreal = 40L)}). Default
an empty list.}

\item{scope}{Character scalar naming what was refused (default \code{"call"}).}
}
\value{
An object of class \code{"pesto_abstention"}: a list with \code{reason},
\code{detail}, \code{diagnostics}, \code{scope} and \code{abstained = TRUE}.
}
\description{
Returns a small classed object signalling that a PESTO reliability gate
declined to produce a result, with a machine-readable \code{reason} a caller
(or the orchestra's \code{is_orchestra_decline()}) can branch on, instead of an
uncaught error or a half-populated result.
}
\examples{
a <- pesto_abstention("degenerate_ensemble",
                      detail = "1 of 40 realisations succeeded",
                      diagnostics = list(iteration = 2L, n_ok = 1L))
is_pesto_abstention(a)

}
