% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{verify_manifest}
\alias{verify_manifest}
\title{Verify the integrity of a manifest}
\usage{
verify_manifest(manifest, ...)
}
\arguments{
\item{manifest}{A \code{pesto_ensemble_manifest}.}

\item{...}{Reserved.}
}
\value{
A list with \code{ok} (\code{TRUE}, \code{FALSE}, or \code{NA} -- see Details),
\code{stored} (the manifest's recorded hash), \code{recomputed} (the hash
computed from current data), and \code{message} (\code{NULL} for verifiable
formats, otherwise an explanation).
}
\description{
Recomputes the SHA-256 hash over \verb{(params, outputs, weights, obs_target, seed)} and compares against the stored \code{data_hash}. Use
this after \code{\link[=read_manifest]{read_manifest()}} to confirm the data files have not been
tampered with or silently re-saved.
}
\details{
When the manifest's \code{format} slot is \code{"csv"}, the on-disk data has
been round-tripped through \code{utils::read.csv()} and IEEE 754 doubles
have been truncated at the formatter's precision (~15-17 digits).
That precision loss is enough to flip the SHA-256 hash, so
\code{verify_manifest()} returns \code{ok = NA} with an explanatory
\code{message} field rather than reporting a spurious \code{FALSE}.
}
\examples{
m <- as_manifest(
  pesto_ies_callback(
    function(t) t \%*\% t(matrix(1, 2L, 2L)),
    matrix(rnorm(20L), 10L, 2L, dimnames = list(NULL, c("a", "b"))),
    c(o1 = 0.1, o2 = -0.2), obs_sd = 0.1, noptmax = 2L, verbose = FALSE
  ),
  seed = 1L
)
verify_manifest(m)$ok
}
