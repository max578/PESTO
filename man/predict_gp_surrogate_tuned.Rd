% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogate.R
\name{predict_gp_surrogate_tuned}
\alias{predict_gp_surrogate_tuned}
\title{Predict from an MLE-Tuned GP Surrogate}
\usage{
predict_gp_surrogate_tuned(gp, X_new)
}
\arguments{
\item{gp}{A surrogate from \code{\link[=train_gp_surrogate_tuned]{train_gp_surrogate_tuned()}}.}

\item{X_new}{Numeric matrix of inputs to predict at.}
}
\value{
The list \code{\link[=predict_gp_surrogate]{predict_gp_surrogate()}} returns (\code{mean}, \code{variance},
\code{uncertainty}), with \code{mean} on the original response scale.
}
\description{
Companion predictor for \code{\link[=train_gp_surrogate_tuned]{train_gp_surrogate_tuned()}}. It reapplies the
per-axis pre-scaling the tuner stored (so an anisotropic surrogate predicts on
the same geometry it was fitted on) and adds the centred response mean back,
returning predictions on the original response scale.
}
\examples{
set.seed(1L)
X <- matrix(runif(40L * 2L), 40L, 2L)
y <- sin(3 * X[, 1]) + 0.5 * X[, 2]^2
gp <- train_gp_surrogate_tuned(X, matrix(y, ncol = 1L))
pred <- predict_gp_surrogate_tuned(gp, X)
}
\seealso{
\code{\link[=train_gp_surrogate_tuned]{train_gp_surrogate_tuned()}}.
}
