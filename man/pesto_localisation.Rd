% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflation_localisation.R
\name{pesto_localisation}
\alias{pesto_localisation}
\title{Covariance Localisation Specification for IES}
\usage{
pesto_localisation(
  method = c("none", "correlation", "distance"),
  taper = c("hard", "soft"),
  threshold = -1,
  n_shuffle = 1L,
  quantile = 0.95,
  distances = NULL,
  par_coords = NULL,
  obs_coords = NULL,
  radius = NULL
)
}
\arguments{
\item{method}{Character. One of \code{"none"} (default), \code{"correlation"},
\code{"distance"}.}

\item{taper}{Character. \code{"hard"} (default) or \code{"soft"}; passed to
\code{\link[=correlation_localisation]{correlation_localisation()}} for \code{method = "correlation"}.}

\item{threshold}{Numeric. Correlation noise floor; negative (default -1)
triggers automatic per-iteration estimation. \code{method = "correlation"}.}

\item{n_shuffle}{Integer \eqn{\ge} 1. Permutation replicates for the
automatic floor (default 1). \code{method = "correlation"}.}

\item{quantile}{Numeric in (0, 1). Quantile of the spurious-correlation
distribution used as the floor (default 0.95). \code{method = "correlation"}.}

\item{distances}{Matrix (npar x nobs) or \code{NULL}. Precomputed
parameter-to-observation distances for \code{method = "distance"}.}

\item{par_coords, obs_coords}{Matrices (npar x d, nobs x d) or \code{NULL}.
Parameter / observation coordinates; Euclidean distances are derived when
\code{distances} is \code{NULL}. \code{method = "distance"}.}

\item{radius}{Numeric (> 0) or \code{NULL}. Gaspari-Cohn localisation radius;
required for \code{method = "distance"}.}
}
\value{
An object of class \code{"pesto_localisation"}.
}
\description{
Builds a control object that tells \code{\link[=pesto_ies_callback]{pesto_ies_callback()}} and
\code{\link[=pesto_ies_filter]{pesto_ies_filter()}} how to taper the ensemble Kalman gain, suppressing the
spurious long-range parameter-observation correlations that a finite
ensemble manufactures. Localisation is applied as a Schur (elementwise)
product on the explicit gain inside \code{\link[=ensemble_solution_localised]{ensemble_solution_localised()}}; the
default \code{method = "none"} leaves the standard SVD update untouched.
}
\details{
\code{"correlation"} is the iterative-ensemble-smoother-native automatic
localisation of Luo & Bhakta (2020): it needs no parameter or observation
coordinates, estimating a noise floor from the ensemble itself and damping
sample correlations that fall below it (see \code{\link[=correlation_localisation]{correlation_localisation()}}).
This is the recommended default for parameter-estimation problems whose
parameters carry no spatial metric. \code{"distance"} is classical
distance-based localisation: a Gaspari-Cohn taper (\code{\link[=gaspari_cohn]{gaspari_cohn()}}) of a
parameter-to-observation distance matrix, for problems where such a metric
exists --- supply either \code{distances} directly or \code{par_coords} + \code{obs_coords}
(Euclidean distances are then computed), together with \code{radius}.
}
\examples{
loc <- pesto_localisation("correlation", taper = "soft")
loc
}
\references{
Luo, X. & Bhakta, T. (2020). Automatic and adaptive localization for
ensemble-based history matching. \emph{Journal of Petroleum Science and
Engineering}, 184, 106559.
}
\seealso{
\code{\link[=pesto_inflation]{pesto_inflation()}}, \code{\link[=correlation_localisation]{correlation_localisation()}}, \code{\link[=gaspari_cohn]{gaspari_cohn()}}.
}
