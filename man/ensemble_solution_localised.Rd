% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{ensemble_solution_localised}
\alias{ensemble_solution_localised}
\title{Localised Ensemble Solution Kernel (explicit-gain GLM form)}
\usage{
ensemble_solution_localised(
  par_diff,
  obs_diff,
  obs_resid,
  weights,
  rho,
  cur_lam,
  eigthresh = 1e-06
)
}
\arguments{
\item{par_diff}{Matrix (npar x nreal). Parameter anomalies.}

\item{obs_diff}{Matrix (nobs x nreal). Observation anomalies.}

\item{obs_resid}{Matrix (nobs x nreal). Observation residuals
(sim - obs); see \code{\link[=ensemble_solution]{ensemble_solution()}} for the sign rationale.}

\item{weights}{Numeric vector (nobs). Observation weights
(1 / sqrt(variance)).}

\item{rho}{Matrix (npar x nobs). Localisation taper in \eqn{[0, 1]},
e.g. from \code{\link[=correlation_localisation]{correlation_localisation()}} or \code{\link[=gaspari_cohn]{gaspari_cohn()}}.}

\item{cur_lam}{Numeric. Current Marquardt lambda.}

\item{eigthresh}{Numeric. Eigenvalue truncation threshold (0-1).}
}
\value{
Matrix (nreal x npar). Negative-direction parameter upgrade,
applied by subtraction.
}
\description{
Computes the IES Gauss-Levenberg-Marquardt update with state-space
covariance localisation applied as a Schur (elementwise) product on the
explicit Kalman gain. The standard SVD kernel \code{\link[=ensemble_solution]{ensemble_solution()}}
works in the reduced observation-anomaly subspace and never forms the
\code{npar x nobs} gain, so it cannot host localisation; this kernel
reconstructs the gain
\deqn{K = \Delta\Theta\, V\, \mathrm{diag}(s)\,
         \mathrm{diag}((s^2 + (\lambda+1))^{-1})\, U^{T},}
(with \eqn{U s V^{T}} the thin SVD of the weight-scaled observation
anomalies) tapers it as \eqn{K \circ \rho}, and applies it to the
weighted residuals.
}
\details{
When \eqn{\rho \equiv 1} the result is identical (to truncation
tolerance) to \code{\link[=ensemble_solution]{ensemble_solution()}} with \code{use_approx = TRUE}; the
prior-scaling null-space correction (\code{upgrade_2}) is not part of the
localised path. The returned matrix follows the same sign convention as
\code{\link[=ensemble_solution]{ensemble_solution()}} --- it is the negative-direction step, applied to
the ensemble by subtraction (\code{par_new = par_old - upgrade}).
}
\examples{
set.seed(1L)
npar <- 4L; nreal <- 20L; nobs <- 6L
par_diff  <- matrix(rnorm(npar * nreal), npar, nreal)
obs_diff  <- matrix(rnorm(nobs * nreal), nobs, nreal)
obs_resid <- matrix(rnorm(nobs * nreal, sd = 0.5), nobs, nreal)
weights   <- rep(1, nobs)
rho       <- matrix(1, npar, nobs)          # no localisation
upg <- ensemble_solution_localised(
  par_diff, obs_diff, obs_resid, weights, rho, cur_lam = 1.0
)
dim(upg)
}
\references{
Chen, Y. & Oliver, D.S. (2013). Levenberg-Marquardt forms of the
iterative ensemble smoother for efficient history matching and
uncertainty quantification. \emph{Computational Geosciences}, 17(4),
689--703.
}
