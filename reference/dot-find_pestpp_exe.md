# Find PEST++ executable

Find PEST++ executable

## Usage

``` r
.find_pestpp_exe(name, user_path = NULL)
```

## Arguments

- name:

  Name of the executable (e.g., "pestpp-ies")

- user_path:

  User-specified path (overrides search)

## Value

Path to executable
